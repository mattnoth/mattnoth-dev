// Orchestrator: parses --dev flag, delegates to esbuild context API for dev server or one-shot production build.
// Coordinates markdown.ts → pages.ts → copy-assets.ts in sequence.
// build-specialist owns this file.
